#include<iostream>
#include<vector>
#include<utility>
#include<string>


using namespace std;


string Generate_Up_To_Num(string Num, string Lan){
    vector<string> response;
    string res;
    int itr = 0;
    for (int i = 0; i < Lan.length(); i++)
    {
        
    }
    return ;
}

int main(){
    int TestCase;
    string Alien_Num, Source_Num, Target_Num;
    vector<string> ans;
    cin >> TestCase;
    for (int i = 0; i < TestCase; i++)
    {
        cin >> Alien_Num >> Source_Num >> Target_Num;
        if (Source_Num.length() == Target_Num.length())
        {
            ans.push_back();
        }
        
        
        
    }
    
    return 0;
}